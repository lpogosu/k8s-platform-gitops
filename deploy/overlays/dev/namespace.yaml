---
apiVersion: v1
kind: Namespace
metadata:
  name: demo-dev
  labels:
    # Enforced by the kubelet on admission; dev runs the same baseline as prod
    # so that a violation surfaces on the first environment, not the last.
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/enforce-version: latest
